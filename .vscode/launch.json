{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch MainApp",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/mainapp",
            "cwd": "${workspaceFolder}/mainapp"
        }
    ]
}